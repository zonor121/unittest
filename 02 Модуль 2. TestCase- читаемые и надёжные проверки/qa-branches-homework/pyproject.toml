[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "netutils"
version = "0.1.0"
description = "Network utilities with port parsing"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]